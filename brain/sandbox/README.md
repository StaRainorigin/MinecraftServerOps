# `brain/sandbox/` · 路径安全校验

执行器在宿主机模拟人工改文件前的权限与路径安全闸。

## 文件职责

| 文件 | 职责 |
|------|------|
| `path_guard.py` | 校验所有指令绝对路径必须锁定在当前实例根目录下；拦截 `../`（目录穿越）与越权操作系统文件（如 `/etc/`），命中即拦截报错 |

## 设计约束

- **实例根目录锁定**：所有文件操作路径必须解析为实例根的子路径，越界即拒绝。
- **目录穿越拦截**：规范化路径后校验前缀，杜绝 `../` 逃逸。
- **系统文件保护**：禁止触碰宿主机系统路径。
